# Good-spine Checklist 定向复审

## Gate verdict

**通过。** 此前 R-01—R-08 均已得到足以约束下游 Epic 的修复；本轮未发现残余 Critical 或 High。架构脊柱现已消除审计所有权冲突、高风险门禁泄漏、技术 seed 定性歧义及 ER 合证基数错误，并补齐恢复、SLI、撤权和部署流向边界。

## 复审范围

本轮仅针对上一轮 rubric gate 的 R-01—R-08 做回归审查，同时确认修复没有削弱模块边界、能力覆盖或运行包络。未修改 `ARCHITECTURE-SPINE.md`。

## R-01—R-08 关闭情况

| ID | 原问题 | 复审结论 | 修复证据 |
| --- | --- | --- | --- |
| R-01 | 审计唯一写所有权与业务同事务提交冲突 | **已关闭** | `AD-2` 明确业务模块只写本地审计事实 outbox，`audit-operations` 唯一拥有集中 `AuditLedger`；`AD-10` 固定本地事务原子边界、幂等归集、失败重放与积压 fail-closed。没有跨模块写表例外。 |
| R-02 | 高风险动作门禁 Deferred 泄漏 | **已关闭** | 新增 `AD-23`，统一 `HighRiskActionPolicy` 输入/输出、默认无映射 deny、审批状态机及执行时复核。Deferred 只保留 actionType 到等级的校方映射，不再允许 Epic 自选机制。 |
| R-03 | 固定版本缺少证据且 fixed/assumption 冲突 | **已关闭** | 标题改为“冷启动技术 Seed”，明确不是不变量、列出 2026-07-16 官方核验来源和实施前兼容矩阵；前端原型版本与生产基线清楚分离，Vite 5 明确禁止直接用于生产。 |
| R-04 | ER Candidate 基数不支持跨类别合证 | **已关闭** | 引入 `CANDIDATE_EVIDENCE` 关联实体，`RULE_EVALUATION` 与 `CANDIDATE` 可形成多来源解释关系；不再把 Candidate 限定为单个 RuleEvaluation。 |
| R-05 | 恢复包络不可验收 | **已关闭** | `AD-14` 固定恢复一致性集、恢复后对账/开流顺序、半年完整恢复演练及 RPO/RTO 证据口径。物理灾备产品继续 Deferred 不会导致业务恢复语义分叉。 |
| R-06 | 99.9% 与 P95 缺少统一测量边界 | **已关闭** | `AD-22` 固定在线请求起止点、事件/待办/身份时延起止点、Asia/Shanghai 业务时段、自然月窗口，并要求计划维护和外部依赖单列而不得静默剔除。 |
| R-07 | 即时撤权与导出权限快照冲突 | **已关闭** | `AD-13` 要求生成前及每次下载按当前授权复核，权限快照只用于审计并与当前权限取交集；`AD-24` 再次禁止持久化明文授权结果。 |
| R-08 | 部署 Mermaid 混淆认证与数据入口 | **已关闭** | 图中 Portal/App 直接调用 web-api，SSO 以虚线表达认证信任/事实；权威数据源改连 worker，用户流、认证信任和接入流已区分。 |

## Good-spine checklist 回归结果

| 检查维度 | 结论 | 说明 |
| --- | --- | --- |
| 真实分歧点完整性 | **通过** | 修复新增的 AD-23—AD-25 进一步固定高风险门禁、事件/读模型和更正级联；未见本 altitude 整维静默。 |
| AD Rule 可执行性 | **通过** | 审计原子边界、作业 fencing、授权重检、门禁状态机均有明确执行与失败语义。 |
| Deferred 不泄漏 | **通过** | 高风险项只延期等级映射；基础设施、协议、产品和版本项均保留稳定行为边界及重访条件。 |
| Named tech verified-current | **通过** | 技术项被正确降格为带日期和官方来源的 seed，并要求实施前复核；不再冒充长期架构不变量。 |
| Brownfield ratification | **通过** | 前端原型快照与生产选择明确分离，后端/部署继续标记为目标假设，没有把不存在的生产工程写成现实。 |
| PRD/UX capability coverage | **通过** | 原 6.1—6.16 与 NFR 映射仍完整；新增 AD 没有削弱既有能力，ER 已支持合证。 |
| 运行与环境包络 | **通过** | 部署角色、环境隔离、恢复一致性、RPO/RTO、演练、SLI、性能容量和观测边界均已决定或有受控 Deferred。 |
| Mermaid 与 seed | **通过** | 三张图在语义上与 AD 一致；技术、拓扑和源码树均清楚标记 seed/assumption，不会与不变量混淆。 |

## 残余非阻塞提示

- **Low：** “业务时段”的具体每日起止时刻仍需由校方运营日历给值，但 `AD-22` 已固定时区、月度窗口和不可静默剔除规则，因此不会允许 Epic 自定义统计语义；可在运行手册/校方配置契约中落值。
- **Low：** 冷启动 seed 的官方来源使用可识别域名/路径而非 Markdown 链接，不影响构建约束，但最终归档时可补成可点击引用以便审计复核。
- **Low：** Mermaid 语义复审通过；最终交付前仍应由实际 Mermaid 渲染器执行机械语法验证。

## 最终结论

**PASS — 无残余 Critical/High。** 上述 3 项 Low 均不构成下游实现分叉，可随最终机械 lint/渲染或校方运行配置一起处理。
